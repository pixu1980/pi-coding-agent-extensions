# The registry and the publish access are explicit rather than inherited.
registry=https://registry.npmjs.org/
access=public

# No dependency cooldown is configured on purpose. This repository consumes the
# newest available dependency versions, so pnpm's minimumReleaseAge is left
# unset. The tradeoff that accepts is recorded in ADR 012.
