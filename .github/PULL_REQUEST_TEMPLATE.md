## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] `pnpm test` and `pnpm test:all` pass
- [ ] `pnpm lint` and `pnpm format:check` pass
- [ ] The affected package's `README.md` is updated if its behavior changed
- [ ] `CHANGELOG.md` is untouched, because the release tool generates it
- [ ] Any new dependency is named below and pinned to an exact version

## New dependencies

<!-- Write "none", or name each one and say why it is worth adding. -->
