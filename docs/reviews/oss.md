# oss review

- **Role**: oss v1
- **Date**: 2026-09-15
- **Scope**: the whole pi-coding-agent-extensions monorepo: the 8 published packages under packages/ plus the repository root (governance, license, release, community, funding surface)

## Lens coverage

Each lens required by this run, applied to the scope, and the findings that answer it. All seven
apply to this scope; none is not applicable.

| Lens | Findings | Verdict |
| ---- | -------- | ------- |
| Governance a new contributor can find and follow | OSS-02, OSS-03, OSS-05 | Fails. No CONTRIBUTING.md, no code of conduct, no GOVERNANCE.md. The only governance artifact that exists is `docs/adr/` with 10 accepted ADRs, and nothing links to it. |
| Contribution path from first issue to merge, and where it stalls | OSS-02, OSS-05, OSS-09 | Fails at the first step. There is no issue template and no path document, and the merge gate is a no-op because `pnpm lint` echoes and `pnpm format` cannot match a file. |
| CLA or DCO the project asks for, and whether it is enforced | OSS-02, OSS-15 | Neither exists, and it is enforced nowhere. `web_commit_signoff_required` is false. Recorded as a deliberate inbound=outbound `MIT` choice, not as an oversight, and made consistent by OSS-15. |
| Funding transparent to donors and independent of one payer | OSS-06 | Fails. There is no funding surface at all, so the project is 100% single-payer with no defined donor relationship. |
| Release cadence contributors can plan around | OSS-07, OSS-12, OSS-16, OSS-17 | Fails. No cadence is documented, there are 0 GitHub Releases, no root changelog, the tree is currently unreleasable, and the dry-run summary misreports. |
| Community whose decisions are documented, not remembered | OSS-05, OSS-12, OSS-14 | Partially passes. `docs/adr/` is genuinely strong for architectural decisions and `docs/handoff/` records session state, but there is no governance record, no public roadmap, and no community venue. |
| Boundaries between maintainers and donors, and who decides on conflict | OSS-05, OSS-06 | Fails. No donors exist and no boundary is written, so the question is currently undefined rather than answered. |

## Decisions taken on this run

Put to the maintainer and answered before the plan was written, so each is a recorded decision
rather than an assumption. They are what turns the CI, contribution-terms, governance and funding
lenses into a shape the plan can execute.

| Question | Decision | Consumed by |
| -------- | -------- | ----------- |
| Should CI be reintroduced, given the earlier deliberate removal of `.github/`? | No. Keep zero CI on purpose and document the gaps as accepted risks. | OSS-17, and the "Deliberately not in this plan" section of the plan |
| Is the uncommitted dependency bump in the working tree intentional? | Yes, intentional and uncommitted. The plan must include committing it properly. | OSS-07 |
| Which inbound license term applies? | Neither CLA nor DCO: inbound=outbound under `MIT`, documented as a deliberate choice. | OSS-02 sub-step 2.2, OSS-15 |
| Which governance model should be documented? | BDFL / single maintainer, written down explicitly. | OSS-05 |
| What should the project do about funding? | Set up a transparent funding surface: GitHub Sponsors, `FUNDING.yml`, documented use of funds, and a maintainer/donor boundary. | OSS-06 |
| Is the absence of `package-lock.json` and of a `packageManager` field intentional? | Yes. pnpm only, with the pnpm lockfile per package; every `package.json` is meant to look that way. | OSS-07, OSS-08 |
| Are `packages: []` at the root and the per-package `overrides`/`allowBuilds` divergence intentional? | Yes. Each package is a standalone pnpm project carrying only the dependencies it needs, and the root is a script runner rather than a workspace. | OSS-08, OSS-10 |

Findings distilled from [the audit](../audits/oss.md). A finding this run no longer reports is
flipped to resolved and kept one run for visibility, then removed.

| Finding | Severity | Status | Evidence                                                                                                                                                                                                                                                                                                                                                | Summary                                                                                                                                                                                                                    |
| ------- | -------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OSS-01 | high | resolved | audit §5: 6 of 8 package dirs had no LICENSE file (pi-ask, pi-cursor, pi-path-picker, pi-reasoning, pi-sessions, pi-statusline); registry tarballs for @pixu1980/pi-ask and @pixu1980/pi-statusline contained no LICENSE while @pixu1980/pi-mcp contains package/LICENSE. The files arrays omitted LICENSE for 6 packages. | Resolved: all 8 packages now ship LICENSE and list it in files, verified by test/license-integrity.test.mjs and by npm pack --dry-run on all 8 tarballs. The pi-mcp fork keeps the upstream copyright notice it inherited, recorded as ADR 011. |
| OSS-02  | high     | open   | audit §6: GitHub community profile health_percentage 42 with contributing MISSING, issue_template MISSING, pull_request_template MISSING; ./github/ISSUE_TEMPLATE returns HTTP 404; root README.md is 47 lines with headings pi-coding-agent-extensions, Structure, Publishing, Development, License and no contribution section.                       | There is no contributor on-ramp at all: no CONTRIBUTING.md, no issue templates, no pull request template, so a newcomer cannot tell how to propose, test or land a change.                                                 |
| OSS-03  | high     | open   | audit §6: code_of_conduct and code_of_conduct_file both MISSING in the community profile; no CODE_OF_CONDUCT.md anywhere in the repository root scan.                                                                                                                                                                                                   | The project has no code of conduct, so it defines no behavioral standard for contributors and no enforcement path.                                                                                                         |
| OSS-04  | high     | open   | audit §6: SECURITY.md MISSING in the root file scan; no SECURITY section in README.md; audit §10 shows 0 of 8 packages with provenance attestations. Only packages/pi-web/README.md contains a Security heading.                                                                                                                                         | There is no security policy and no private channel for reporting vulnerabilities, which matters more than usual here because the packages execute inside the user's agent process.                                         |
| OSS-05  | high     | open   | audit §6: git log --merges --oneline \| wc -l = 0; commit authors collapse to one person across three email addresses; docs/ contains only adr and handoff. GOVERNANCE.md and MAINTAINERS.md MISSING. docs/adr/ holds 10 accepted ADRs plus the index.                                                                                                  | Who decides what is undocumented: 236 commits, 0 merge commits and 1 contributor mean the real model is single-maintainer, but no GOVERNANCE.md states it, so a newcomer cannot tell who reviews or who breaks a tie.      |
| OSS-06  | high     | open   | audit §6 and §11: no FUNDING.yml, no funding field in any of the 9 package.json files, no sponsors surface; GitHub repo homepage is None. Zero releases, one contributor.                                                                                                                                                                               | The project has no funding surface of any kind and is 100% single-payer, so no donor can contribute and no donor expectation is defined.                                                                                   |
| OSS-07  | high     | open   | audit §3: git status shows 9 modified package.json and 2 deleted package-lock.json with mtimes 20:46-20:49 during the run; audit §4 shows pnpm release:dry marks all 8 packages release-worthy; scripts/release.mjs line 117 aborts on a dirty tree. Maintainer decision on this run: the bump is intentional and uncommitted, and the removal of the npm lockfiles and of the `packageManager` field is part of it.                                                                                                    | The intentional dependency bump is uncommitted, so the tree is dirty: the release path refuses to run and all 8 packages are detected as release-worthy.                                                 |
| OSS-08 | medium | open | audit §3: git status shows D packages/pi-path-picker/package-lock.json and D packages/pi-reasoning/package-lock.json. audit §8: the root pnpm-workspace.yaml declares packages: [] and each of the 8 packages carries its own pnpm-workspace.yaml and pnpm-lock.yaml. Maintainer decision on this run: standalone packages by design, pnpm only, no `packageManager` field anywhere. | The pnpm-only policy is undocumented and unenforced: two npm package-lock.json files had already crept into what are standalone pnpm packages, and nothing stops the next contributor from adding one. |
| OSS-09  | high     | open   | audit §8: pnpm format errors with 'No files matching the pattern were found: packages/*/src/**/*.ts' and exits 2, packages/*/src does not exist, prettier is absent from root devDependencies, and the lint script is echo 'No linter configured yet'. docs/handoff/04 records that the previous ESLint pass lived in /tmp and no dependency was added. | The repository has no working style gate: pnpm format targets a directory that does not exist and pnpm lint is a no-op, so nothing enforces the house style a contributor is asked to follow.                              |
| OSS-10  | medium   | open   | audit §9: 12 lines reading 'set this to true or false' across packages/pi-ask/pnpm-workspace.yaml, packages/pi-path-picker/pnpm-workspace.yaml and packages/pi-web/pnpm-workspace.yaml under allowBuilds; pnpm install --lockfile-only --dry-run in pi-ask exits 0, so the invalid values are not rejected.                                             | Three packages carry unfilled template placeholders as configuration values, so their build-script policy silently does nothing.                                                                                           |
| OSS-11  | medium   | open   | audit §10: packages/pi-mcp/lib/_app-bridge.bundle.js is 295508 bytes, tracked in git and present in the npm tarball as package/lib/_app-bridge.bundle.js; no package defines a build script; the file is served at runtime from lib/_ui-server.ts line 362 and referenced by lib/_host-html-template.ts line 4.                                          | pi-mcp publishes a 295 KB minified bundle that no script in the repository builds, so neither a contributor nor a security reviewer can review what ships or reproduce it.                                                 |
| OSS-12  | medium   | open   | audit §6: GitHub releases count is 0; no root CHANGELOG.md exists; docs/ has no roadmap directory; audit §10 shows releases are cut by hand from a developer machine with pnpm release. Per-package CHANGELOG.md files exist but only pi-mcp and pi-statusline ship one in the npm tarball.                                                              | There is no release cadence and no release notes surface, so a contributor or user cannot plan around releases or see what changed.                                                                                        |
| OSS-13  | medium   | open   | audit §12: .npmrc line 1, packages/pi-path-picker/lib/_provider.ts lines 121-122, and scripts/test-all.mjs lines 86 and 88 carry Italian prose, including the aggregator's summary line and its failure label.                                                                                                                                            | Repository-owned files contain Italian prose, contradicting the project's own rule that every artifact except chat is written in English.                                                                                  |
| OSS-14  | medium   | open   | audit §6: topics is [], homepage is None, has_wiki is True, has_discussions is False, open_issues_count is 1 with the only open item being a stale dependabot PR, and 0 GitHub Releases. Package manifests do carry homepage, bugs and keywords, so only the GitHub-side metadata is missing.                                                           | The repository's public metadata is incomplete, so the project is hard to find and the community venues it does have are unused.                                                                                           |
| OSS-15  | medium   | open   | audit §6: git log --format='%aE' \| sort \| uniq -c gives 234 commits under the personal address, 1 under the work address and 1 under the GitHub noreply address; all three addresses are listed in the raw evidence.                                                                                                                                                                                   | Contributor attribution is inconsistent: a single contributor commits under three different email identities, which muddies authorship and would break any future sign-off requirement.                                    |
| OSS-16  | low      | open   | audit §4: the dry-run ends with 'released: 0, skipped: 0' for 8 packages because released is only incremented on the non-dry path in scripts/release.mjs. audit §11: pi-ask, pi-path-picker, pi-reasoning, pi-sessions and pi-statusline declare no engines.                                                                                            | The release script reports misleading dry-run totals and 5 of 8 packages omit an engines field, so the release output and the metadata disagree with reality.                                                              |
| OSS-17  | info     | open   | audit §10: all 8 packages report attestations NO; scripts/release.mjs runs npm publish --access public and git push --follow-tags from the local machine; no .github/workflows exists. Maintainer decision on this run: keep zero CI on purpose and document the gaps as accepted risks.                                                                 | Accepted risk, recorded by maintainer decision on this run: the repository has no CI, so 0 of 8 packages carry provenance attestations and releases are published from a developer machine with a personal npm credential. |
| OSS-18  | info     | open   | audit §6: issue #2 is an open dependabot pull request bumping @vitest/mocker 4.1.10 to 5.0.0 in packages/pi-mcp, reporting 'across 0 directory'; .github/ does not exist; the remote branch dependabot/npm_and_yarn/packages/pi-mcp/npm_and_yarn-8bd3e5320a still exists. Maintainer decision on this run: no CI steps in the plan.                     | Accepted risk, recorded by maintainer decision on this run: dependency updates have no automation because .github/dependabot.yml was removed, and dependabot pull request #2 has been open since 2026-09-11.               |

## Accepted risks

OSS-17 and OSS-18 are findings whose remediation the maintainer explicitly declined on this run.
They stay in the table above as `open` because the underlying exposure is real and unmitigated;
what the plan carries for them is documentation of the decision, not a fix. The exposure is
restated here so no reader can mistake silence for resolution.

- **OSS-17**: no CI, 0 of 8 packages provenance-attested, releases published from a developer
  machine with a personal npm credential, tags lightweight and unsigned.
- **OSS-18**: no dependency-update automation, and dependabot pull request #2 open since
  2026-09-11, on a repository whose only open item it is.
